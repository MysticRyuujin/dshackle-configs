upstreams:
  - id: anyblock
    chain: ethereum
    role: fallback
    priority: 70
    options:
      disable-validation: true
    connection:
      ethereum:
        rpc:
          url: "https://api.anyblock.tools/ethereum/ethereum/mainnet/rpc/${ANYBLOCK_KEY}"
